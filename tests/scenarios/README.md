# Scenario harness skeleton

This folder contains scenario-level tests that simulate controller/node interactions.

Planned expansions:
- V2 mode envelope-only traffic
- Conflict arbitration under burst command issuance
- Graceful shutdown during active traffic
