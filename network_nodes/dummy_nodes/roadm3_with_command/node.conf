# Register IP address
ip_address = aic_register

# port to register with register
register_port = 5558

# Node ID - must be unique (ROADM3 with command = cell_id 8)
node_id = 8

# Port to publish measurements
pub_port = 5575
