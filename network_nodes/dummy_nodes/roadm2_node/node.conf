# Register IP address
ip_address = aic_register

# port to register with register
register_port = 5558

# Node ID - must be unique (ROADM2 = cell_id 7)
node_id = 7

# Port to publish measurements
pub_port = 5574
