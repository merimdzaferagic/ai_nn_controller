# Register IP address
ip_address = aic_register

# port to register with register
register_port = 5558

# Node ID - must be unique (ROADM1 = cell_id 4)
node_id = 4

# Port to publish measurements
pub_port = 5564
